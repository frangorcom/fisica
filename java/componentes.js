document.addEventListener('DOMContentLoaded', function () {
  var typed = new Typed('#comp', {
    strings: ['¿para qué es útil eso?', '¿Cómo se calculan esos vectores?'], // Textos que se mostrarán
    typeSpeed: 50, // Velocidad de escritura
    backSpeed: 25, // Velocidad de borrado
    loop: true, // Repetir el efecto
  });
});